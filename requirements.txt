# Hypatia's Hoard (local server). Pinned to the versions verified in the build environment
# (Python 3.11; 3.11+ required, Luis runs 3.13).
fastapi==0.141.1
uvicorn==0.54.0
pydantic==2.13.5
starlette==1.7.0
httpx==0.28.1
mcp==1.30.0
pypdf==6.19.0
pytest==9.1.1
cryptography==50.0.1
